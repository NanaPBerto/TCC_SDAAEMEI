<div class="container my-5">
  <div class="atividade-destaque card shadow-lg border-0 p-4 mx-auto" style="max-width: 900px; border-radius: 20px;">
    <div class="row g-4 align-items-center">
      <div class="col-md-5 text-center">
        {{#if atividade.imagemBase64}}
          <img src="{{atividade.imagemBase64}}" alt="Imagem da atividade" class="img-fluid rounded-4 shadow mb-3" style="max-height:320px; object-fit:cover; border:4px solid #7E57C2;">
        {{else}}
          <div class="atividade-sem-imagem d-flex align-items-center justify-content-center bg-light rounded-4 mb-3" style="height: 220px;">
            <i class="fas fa-music fa-4x text-secondary"></i>
          </div>
        {{/if}}
        {{#if atividade.desenvolvedorNome}}
          <div class="mt-3">
            <span class="badge rounded-pill bg-primary px-3 py-2" style="font-size:1rem;">
              <i class="fas fa-user me-2"></i>Desenvolvedor: {{atividade.desenvolvedorNome}}
            </span>
          </div>
        {{/if}}
      </div>
      <div class="col-md-7">
        <h2 class="atividade-titulo mb-2" style="color:#7E57C2; font-weight:700;">{{atividade.nome}}</h2>
        <div class="atividade-categoria mb-3">
          <span class="badge bg-secondary me-2" style="font-size:1rem;">
            <i class="fas fa-tag me-1"></i>{{atividade.tipo.nome}}
          </span>
          <span class="badge bg-warning text-dark me-2" style="font-size:1rem;">
            <i class="fas fa-users me-1"></i>{{atividade.vagas}} integrantes
          </span>
          <span class="badge bg-success me-2" style="font-size:1rem;">
            <i class="fas fa-clock me-1"></i>{{atividade.duracao}}
          </span>
          <span class="badge bg-info text-dark" style="font-size:1rem;">
            <i class="fas fa-star me-1"></i>{{atividade.classificacao}}
          </span>
        </div>
        <div class="atividade-info mb-3">
          <p class="mb-2"><strong>Descrição:</strong> <span class="text-muted">{{atividade.descricao}}</span></p>
          <p class="mb-2"><strong>Objetivo:</strong> <span class="text-muted">{{atividade.objetivo}}</span></p>
          <p class="mb-2"><strong>Indicação:</strong> <span class="text-muted">{{atividade.indicacao}}</span></p>
          <p class="mb-2"><strong>Recursos:</strong> <span class="text-muted">{{atividade.recursos}}</span></p>
          <p class="mb-2"><strong>Condições:</strong> <span class="text-muted">{{atividade.condicoes}}</span></p>
          {{#if atividade.obs}}
            <p class="mb-2"><strong>Observações:</strong> <span class="text-muted">{{atividade.obs}}</span></p>
          {{/if}}
        </div>
        <div class="atividade-anexos mt-4">
          {{#if atividade.partituraBase64}}
            <div class="mb-3">
              <span class="fw-bold"><i class="fas fa-file-alt me-2"></i>Partitura:</span>
              <a href="{{atividade.partituraBase64}}" target="_blank" class="btn btn-outline-primary btn-sm ms-2">Visualizar Partitura</a>
            </div>
          {{/if}}
          {{#if atividade.musicaBase64}}
            <div class="mb-3">
              <span class="fw-bold"><i class="fas fa-music me-2"></i>Áudio:</span>
              <audio controls class="w-100 mt-2">
                <source src="{{atividade.musicaBase64}}">
                Seu navegador não suporta o elemento de áudio.
              </audio>
            </div>
          {{/if}}
        </div>
      </div>
    </div>
  </div>
</div>
<style>
/* ...adicione estilos extras se desejar, mas o style.css já cobre a maioria dos casos... */
.atividade-destaque .atividade-titulo {
  font-size: 2rem;
  letter-spacing: 0.5px;
}
.atividade-destaque .atividade-categoria .badge {
  font-size: 1rem;
  margin-bottom: 0.2rem;
}
.atividade-destaque .atividade-info p {
  font-size: 1.08rem;
}
.atividade-destaque .atividade-anexos .btn {
  font-size: 0.98rem;
}
</style>
